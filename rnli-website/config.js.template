// Runtime configuration — generated by Docker entrypoint from environment variables.
// To override defaults, set these env vars on the rnli-website container in docker-compose.yml.
window.APP_CONFIG = {
  agentCardUrl:        '${AGENT_CARD_URL}',
  oidcUrl:             '${OIDC_URL}',
  clientId:            '${OIDC_CLIENT_ID}',
  redirectUri:         '${REDIRECT_URI}',
  visitedStationsUrl:  '${VISITED_STATIONS_URL}',
};
